# ─────────────────────────────────────────────
# secret.yaml — PROD environment
#
# THIS FILE IS A PLACEHOLDER SKELETON ONLY.
# The real DB_PASSWORD is injected at deploy time by the Jenkinsfile.
# See k8s/dev/secret.yaml for full explanation.
# ─────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: app-db-secret
  namespace: prod
  labels:
    app: mnc-app
    environment: prod
  annotations:
    managed-by: "jenkins-pipeline"
type: Opaque
data: {}
